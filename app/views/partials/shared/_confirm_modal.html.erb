<div class="fixed z-20 inset-0 items-center justify-center bg-black text-base bg-opacity-50 transition-opacity duration-300 hidden" data-confirmation-target="modal">
  <!-- Modal Content -->
  <div class="bg-white rounded-lg shadow-lg p-4 w-full relative z-20 max-w-md">
    <h2 class="text-lg font-semibold text-black mb-4">Confirm Deletion</h2>
    <p class="text-gray-700 mb-4">Are you sure you want to delete this item?</p>
    <div class="flex justify-end">
      <button id="cancel-button" class="text-gray-500 hover:text-gray-700 px-4 py-2 rounded" data-action="confirmation#toggle">Cancel</button>
      <%= button_to "Confirm", "#{path_url}", method: :delete, class: "bg-red-500 text-white hover:bg-red-600 px-4 py-2 ml-2 rounded", data: { action: "confirmation#toggle confirmation#removeDetail" } %>
    </div>
  </div>
</div>
