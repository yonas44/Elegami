<div id="deleteModal" class="hidden text-black fixed inset-0 backdrop-blur-sm items-center justify-center z-50">
  <!-- Modal Content -->
  <div class="bg-white p-5 rounded-lg shadow-lg">
    <p>Are you sure you want to delete?</p>
    <div class="mt-4 flex justify-end">
      <button id="cancelButton" class="px-4 py-2 bg-gray-400 text-white rounded hover:bg-gray-600" data-action="project#handleConfirm">Cancel</button>
      <button id="confirmButton" class="ml-2 px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600" data-action="project#handleConfirm">Delete</button>
    </div>
  </div>
</div>
